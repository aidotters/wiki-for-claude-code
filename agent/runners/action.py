"""GitHub Actions エントリポイント（Phase 3 で実装予定のスタブ）。"""

from __future__ import annotations


def main() -> int:
    raise NotImplementedError(
        "agent/runners/action は Phase 3 で実装予定。"
        "現時点ではローカル `agent` コマンド（agent/runners/local）を使用してください。"
    )


if __name__ == "__main__":
    raise SystemExit(main())
